syntax = "proto3";

package com.roleypoly.auth.backend;

service AuthBackend {
    rpc GetSessionChallenge(UserSlug) returns (AuthChallenge) {}
}

message UserSlug {
    string userID = 1;
}

message AuthChallenge {
    string userID = 1;
    string magicUrl = 2;
    string magicWords = 3;
}